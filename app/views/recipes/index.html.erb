<div class="containter">

<div class="row">
<div class="col-md-3">

    <% @recipes.each do |recipe| %>

<a href="/recipes/<%= recipe.id %>">
    <%= image_tag recipe.recipe_img.url(:recipe_index), class: "recipe" %></a>
    <h2> <%= link_to recipe.title, recipe_path(recipe) %></h2>
</div>
<% end %>

    </div>
</div>
</div>
</div>